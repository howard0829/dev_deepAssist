# DeepAssist 서버 의존성
# 설치:  pip install -r requirements.txt
#
# 주: vLLM·LiteLLM은 이 리포 밖에서 별도로 실행하므로 여기 포함하지 않는다.
#     (DeepAssist는 .env 의 ANTHROPIC_BASE_URL 로 외부 LiteLLM 에 접속한다)

# --- 웹/WS 서버 ---
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
python-dotenv>=1.0

# --- Core agent ---
# Claude Code를 라이브러리로 감싼 SDK. 내부에 native 바이너리 동봉.
claude-agent-sdk>=0.1.0
